# 项目交接与完整计划

本文档用于在中断或重新打开项目后快速恢复开发。它记录当前实现、已确认需求、约束、运行方式和后续任务顺序。

## 当前状态

项目是一个桌面浏览器体素沙盒游戏，采用 TypeScript、Vite、Three.js 与 pnpm 工作区。当前已有一个可操作的单机原型：

- 确定性种子地形：草地、泥土、石头、沙子、基岩、静态水与跨区块树木。
- 无限与固定边界世界接口；视距内区块加载与远处网格释放。
- 第一/第三人称、鼠标视角、WASD、基础碰撞、跳跃与飞行。
- 左键破坏、右键放置、数字键选择方块、方块颜色粒子。
- 主界面、Esc 暂停、继续、返回主界面及画布右键/拖拽手势拦截。
- IndexedDB 本地存档：同一种子支持多个命名存档，保存玩家位置和已修改区块差异。
- 原创代码生成的 16×16 像素图集；草方块、木头等支持顶面/侧面/底面纹理标识，树叶有 alpha 裁切孔洞。

当前远程主分支包含最新提交 `336b173`。本地参考截图不会提交到仓库，已被 `.gitignore` 忽略。

## 已确认需求

- 技术栈：TypeScript + Vite + Three.js；pnpm 工作区；严格 TypeScript、ESLint、Prettier、Vitest。
- 目标平台：桌面浏览器，不要求触控兼容。
- 世界：种子生成、接近经典方块沙盒的区块处理；同时支持无限和固定尺寸地图；景观偏大；不生成矿物。
- 方块：先实现破坏、放置、选择栏与方块色粒子；水必须出现在生成地图中；暂不做合成和熔炉。
- 移动：正常移动、跳跃、飞行，第一/第三人称；不做伤害。
- 模组：本地受信任 JavaScript/TypeScript 模组优先；未来支持 JSON 数据；可扩展方块、材质、生成、配方、实体、UI 和行为。
- 联机：未来支持两个活动房间、每房 1–5 人；房间开始前校验模组 ID、版本和哈希。
- 存储：服务器磁盘预算 1 GB、内存可用约 1 GB；磁盘满时拒绝新增数据并提示，不自动删除世界。

## 运行与检查

本机可能未将 `pnpm` 加入 PATH，因此统一使用 Corepack：

```bash
corepack pnpm install
corepack pnpm dev
```

完整质量门禁：

```bash
corepack pnpm format:check
corepack pnpm lint
corepack pnpm typecheck
corepack pnpm test
corepack pnpm build
```

开发时请打开可见浏览器窗口验证网页交互，而不仅使用后台截图。开发服务默认地址为 `http://127.0.0.1:5173`。

## 目录与边界

| 路径                  | 职责                                                       |
| --------------------- | ---------------------------------------------------------- |
| `apps/web`            | 网页入口、菜单、输入、HUD、IndexedDB 调用                  |
| `packages/core`       | 方块、区块、坐标、种子地形、水流规则、世界边界、世界元数据 |
| `packages/renderer`   | Three.js 网格、图集、相机、玩家、粒子、区块流送            |
| `packages/storage`    | IndexedDB 世界存档与存档数据类型                           |
| `packages/mod-api`    | 后续创建：稳定模组公开 API                                 |
| `packages/mod-loader` | 后续创建：本地模组发现、哈希与加载                         |
| `apps/server`         | 后续创建：WebSocket 房间和服务器持久化                     |

`core` 绝不能依赖 DOM、Three.js、IndexedDB 或网络。模组只能依赖未来的 `mod-api`，不能导入内部实现。

## 推荐后续顺序

### 1. 材质与性能（当前阶段）

1. 完善原创像素图集：为各方块增加更清楚但不密集的明暗、边缘与材质特征。
2. 支持图集从资源包或模组替换，避免把图片路径硬编码在网格中。
3. 加入低/中/高画质预设：视距、像素比、粒子数量与每帧网格预算。
4. 优先处理生产包约 505 kB 的 Vite 警告，例如按 Three.js/游戏模块拆包。
5. 需要更高视距时实现贪心网格、分帧网格构建与区块缓存上限。

### 2. 水体与天空（已列为必做）

1. ~~天空渐变、像素云层和随时间变化的光照。~~ 已完成昼夜天空：渐变穹顶、方块像素日月、星点与随时间变化的光照（`packages/renderer/src/sky.ts` 与纯函数 `sky-cycle.ts`）；像素云层仍待补充。
2. ~~水面独立透明材质、轻微流动或波纹效果。~~ 已完成：全区块共享的透明水面材质与无缝波纹纹理逐帧滚动（`packages/renderer/src/water-material.ts`），水面按水位分级下沉呈阶梯坡（`chunk-mesh.ts`），相邻水位差处补画露出的侧面条带。
3. 水下雾、偏蓝滤镜和可见距离衰减。
4. 入水状态、游泳/下潜速度、浮力和离水反馈。
5. ~~**水流扩散衰减（已列为必做）**~~ 已完成：纯函数水位规则位于 `packages/core/src/water-flow.ts`，蔓延上限 `MAX_WATER_LEVEL = 3`（曼哈顿 3 格）且每扩散一格水位下降一级，双管齐下压低横向扩散；规则由确定性状态驱动（不读时间/随机），`water-flow.test.ts` 含 13 个单元测试（含下落水柱不横向摊开的回归用例）。运行时调度器 `packages/renderer/src/water-flow-controller.ts` 每 0.2s 逐 tick 推进活跃前沿并只重建水网格；蔓延水不进存档（仅玩家放置的水源作为方块修改存档），读档后水源自动重新蔓延。

这些应保持为渲染与玩家状态扩展，不修改确定性地形生成规则；唯一例外是水流扩散属于确定性世界模拟，需按上面第 5 点的约束实现。

### 3. 方块与交互完善

1. 选择栏可视化与方块数量/物品栏模型。
2. 防止在玩家碰撞盒内放置方块。
3. 方块破坏时间、音效、掉落物和粒子对象池。
4. 树叶实体碰撞：当前树叶只具有透明渲染语义，尚未作为玩家阻挡物；这是已记录任务。

### 4. 存档完善

1. 存档重命名、删除、导出和导入。
2. 存档格式版本迁移与损坏提示。
3. 保存失败提示、浏览器存储配额提示和世界缩略信息。
4. 避免在同一存档未恢复完成前进行修改；当前恢复是异步进行的。

### 5. 模组系统

1. 创建 `@gm/mod-api`，先定义版本、模组清单、注册表和生命周期。
2. 创建 `@gm/mod-loader`，开发模式从 `mods/` 加载；发布版本使用文件夹选择或模组包导入。
3. 支持方块、纹理标识、生成钩子、物品、配方、实体、UI 注册。
4. 保存模组清单与哈希；进入联机房间前强制一致。

### 6. 联机与服务器

1. 创建轻量 Node.js WebSocket 服务，仅维护两个活动房间。
2. 服务端保存房间、玩家状态与已修改区块增量；不要在服务器常驻完整种子地形或渲染数据。
3. 同步视距附近区块差异和玩家位置；初期只做合理操作范围校验。
4. 按房间非活动状态写盘并释放内存；磁盘满时拒绝写入而不自动删除。

## 已知限制与注意事项

- **参考图与版权**：参考图只用于观察色调、天空、水面、方块密度和树叶轮廓。不要复制原版纹理、UI 或音效；使用原创或许可兼容资源，并记录来源与许可证。
- **图像生成**：当前会话没有内置图像生成工具。若未来要生成新的位图资产，可使用可用的图像生成工作流；必须将最终项目资产放入工作区并保留许可证信息。
- **右键手势**：网页已在捕获阶段阻止画布右键菜单、辅助点击、拖拽与指针移动默认行为。如果仍出现轨迹，通常来自浏览器扩展或鼠标手势软件，它们会先于网页事件处理，需由用户在对应工具中关闭。
- **树叶**：已使用透明像素和 `alphaTest` 形成视觉孔洞，但区块网格仍会为相邻树叶生成面；性能优化时可改用专门的叶片网格或合并规则。
- **水与天空**：昼夜天空与水面波纹/流动已实现；尚未实现水下视角、游泳与像素云层。地形生成的海洋/湖泊仍为静态水源，仅被玩家挖开或接水后才流动。
- **存档**：当前用浏览器 IndexedDB 保存；清除站点数据会清除本地存档。存档列表不支持删除和导出。
- **浏览器测试**：可见浏览器测试窗口要保留给用户确认；启动 Vite 服务时使用隐藏终端，不强制关闭用户窗口。
- **Git**：推送偶尔因 GitHub 网络断开失败。不要丢弃本地提交；网络恢复后执行 `git push origin main` 重试。
- **代码风格**：保持中文文档和注释为主；不使用隐式 `any`；公开 API 明确类型；文件名 `kebab-case`；避免跨层依赖。

## 相关文档

- [架构](architecture.md)
- [开发规范](development-guide.md)
- [地形与渲染](terrain-and-rendering.md)
- [本地存档](local-saves.md)
- [材质系统](materials.md)
- [模组设计](modding.md)
- [服务器与存档](server-and-storage.md)
- [实施路线图](roadmap.md)
