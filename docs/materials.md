# 材质系统

每个方块定义包含 `textures.top`、`textures.side` 和 `textures.bottom` 三个纹理标识。它们将是材质图集、资源包和未来模组覆盖的稳定引用；方块网格不应硬编码某个图片文件名。

当前渲染会以代码生成原创 16×16 像素图集，并在网格构建时按面写入 UV；顶点色继续提供方向明暗。之后可由模组注册新的纹理标识和图集资源。

水将使用独立透明材质。水下雾、折射与移动反馈已记录为后续独立效果，避免与基础图集加载耦合。
